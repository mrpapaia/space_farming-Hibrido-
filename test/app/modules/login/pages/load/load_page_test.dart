import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:space_farming_modular/app/modules/login/pages/load/load_page.dart';

void main() {
  testWidgets('LoadPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(LoadPage(title: 'Load')));
    //  final titleFinder = find.text('Load');
    //  expect(titleFinder, findsOneWidget);
  });
}
