import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:space_farming_modular/app/modules/relatorio/relatorio_page.dart';

void main() {
  testWidgets('RelatorioPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RelatorioPage(title: 'Relatorio')));
    //  final titleFinder = find.text('Relatorio');
    //  expect(titleFinder, findsOneWidget);
  });
}
