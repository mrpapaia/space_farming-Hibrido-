import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:space_farming_modular/app/modules/relatorio/relatorio_controller.dart';
import 'package:space_farming_modular/app/modules/relatorio/relatorio_module.dart';

void main() {
  initModule(RelatorioModule());
  // RelatorioController relatorio;
  //
  setUp(() {
    //     relatorio = RelatorioModule.to.get<RelatorioController>();
  });

  group('RelatorioController Test', () {
    //   test("First Test", () {
    //     expect(relatorio, isInstanceOf<RelatorioController>());
    //   });

    //   test("Set Value", () {
    //     expect(relatorio.value, equals(0));
    //     relatorio.increment();
    //     expect(relatorio.value, equals(1));
    //   });
  });
}
